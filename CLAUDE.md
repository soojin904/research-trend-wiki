# 연구 동향 위키

위키 운영 규칙은 [[SCHEMA.md]]를 따른다.

## 슬래시 명령어

| 명령어 | 설명 |
|--------|------|
| `/wiki:ingest [파일]` | raw/ 소스를 위키에 통합. 인수 없으면 미등록 파일 자동 탐지 |
| `/wiki:query <질문>` | 위키를 검색해 답변 합성. 가치 있는 답은 페이지로 저장 |
| `/wiki:lint` | 고아 페이지, 깨진 링크, 누락 개념 등 위키 건강 점검 |

플러그인 위치: `.claude/plugins/wiki/`

## Python 환경
PDF 처리 시 `D:\soojin\.venv` 가상환경 사용 (PyMuPDF 설치됨).

## 기본 규칙
- `raw/` 파일은 수정 금지
- 위키링크: `[[파일경로|표시텍스트]]`
- 신규 페이지 생성 시 즉시 `index.md` 업데이트
- 모든 작업 후 `log.md`에 기록
